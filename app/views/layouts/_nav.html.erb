<nav class="navbar navbar-light bg-light">
  <%= link_to 'TestGuru', root_path, class: "navbar-brand" %>
  <% if user_signed_in? %>
    <ul class="nav mr-auto">
      <% if current_user&.admin?%>
          <li calss="nav-item">
            <%= link_to "Admin panel", admin_tests_path, class: "btn btn-link" %>
          </li>
      <% end %>
      <li class="nav-item">
        <%= link_to "Feedback", new_feedback_path, class: "btn btn-link" %>
      </li>
      <li class="nav-item">
        <%= link_to "Your badges", user_badges_path, class: "btn btn-link" %>
      </li>
      <li class="nav-item">
        <%= link_to "All badges", all_badges_path, class: "btn btn-link" %>
      </li>
  </ul>
<% end %>
  <span class='navbar-text'>
    <% if user_signed_in? %>
      Welcome, <b><%= current_user.email%></b> Guru |
      <%= link_to 'Logout', destroy_user_session_path , method: :delete %>
    <% else %>
      <%= link_to 'Login', new_session_path(resource_name) %>
      <%= link_to 'Register', new_registration_path(resource_name) %>
    <% end %>
  </span>
</nav>
